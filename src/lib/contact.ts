export const CONTACT_EMAIL = "jaydenloring05@gmail.com";

// Where pilot applications are emailed when the form cannot save them (for
// example before the pilot_applications table exists). Change this one line to
// route the fallback to a different inbox.
export const PILOT_FALLBACK_EMAIL = CONTACT_EMAIL;
